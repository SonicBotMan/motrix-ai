// commands/config.rs — Config read/write Tauri commands.
// Placeholder for future config-related commands exposed to the frontend.
// Currently, config management lives entirely in TypeScript (packages/core).
